apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Bootstrap installation
# Step 1: Install GitOps operator
# Step 2: Create ArgoCD Applications that manage everything else

resources:
  - 00-gitops
  - ../argocd-apps

# Common metadata for bootstrap
commonAnnotations:
  description: "HCP on SNO - GitOps Bootstrap"
