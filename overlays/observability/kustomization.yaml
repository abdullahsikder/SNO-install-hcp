apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../../components/observability

# Add environment-specific patches here if needed
# For example, custom Grafana admin credentials, additional dashboards, etc.
